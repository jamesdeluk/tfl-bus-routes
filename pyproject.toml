[project]
name = "tfl-bus-routes"
version = "0.1.0"
description = "Visual exploration of Transport for London bus routes."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "marimo>=0.24.0",
    "pydeck>=0.9.1",
    "streamlit>=1.42.2",
    "streamlit-folium>=0.27.4",
]
